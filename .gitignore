# Dependencies
node_modules
.pnpm-store

# Next.js
.next
out

# Production
dist
build

# Env
.env
.env.local
.env.*.local

# Vercel CLI state: the project link, `vercel build` output, and the env file
# `vercel pull` writes with production's secrets in plain text (drk-deploy
# deletes that one after each run, F-47).
.vercel

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode
.idea
*.swp
.DS_Store

# Testing
coverage
playwright-report
test-results
.vitest-cache
.stryker-tmp/

# Generated
src/db/schema/generated.ts

# openapi-generator JAR cache (openapitools.json storageDir); every copy is
# SHA-256-verified by scripts/verify-openapi-generator-jar.ts before use.
.cache/

# TypeScript
*.tsbuildinfo

# Sentry (build plugin artifacts)
.env.sentry-build-plugin
.sentryclirc

# Mutation reports (Stryker JSON/HTML output)
reports/
