/* tslint:disable */
/* eslint-disable */
/**
 * DevResponse Administrator API
 * Administrator console API (`/api/administrator`). Authenticate EITHER with the Better Auth **session cookie** (send credentials with the request; every **mutation** then also requires an `Origin` or `Referer` header matching a trusted origin — the CSRF guard — which a browser sets itself and a server-side caller must add) OR with a **bearer** API key / JWT, whose effective authority is the intersection of its scopes and the permissions of its owner and which is exempt from the origin guard. The one exception is `DELETE /users/{id}/impersonate`, which bypasses the permission guard by design and is therefore **cookie-session only** (see that operation). Org admins are scoped to their own organization (out-of-scope resources return 404, not 403). Errors use `{ error, message, requestId }`; every response carries an `x-request-id` header.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { InvitationItem } from './InvitationItem';
import {
    InvitationItemFromJSON,
    InvitationItemFromJSONTyped,
    InvitationItemToJSON,
    InvitationItemToJSONTyped,
} from './InvitationItem';
import type { SortSpec } from './SortSpec';
import {
    SortSpecFromJSON,
    SortSpecFromJSONTyped,
    SortSpecToJSON,
    SortSpecToJSONTyped,
} from './SortSpec';

/**
 * 
 * @export
 * @interface InvitationList
 */
export interface InvitationList {
    /**
     * 
     * @type {Array<InvitationItem>}
     * @memberof InvitationList
     */
    items: Array<InvitationItem>;
    /**
     * 
     * @type {number}
     * @memberof InvitationList
     */
    page: number;
    /**
     * 
     * @type {number}
     * @memberof InvitationList
     */
    pageSize: number;
    /**
     * 
     * @type {number}
     * @memberof InvitationList
     */
    total: number;
    /**
     * 
     * @type {Array<SortSpec>}
     * @memberof InvitationList
     */
    sort?: Array<SortSpec>;
}

/**
 * Check if a given object implements the InvitationList interface.
 */
export function instanceOfInvitationList(value: object): value is InvitationList {
    if (!('items' in value) || value['items'] === undefined) return false;
    if (!('page' in value) || value['page'] === undefined) return false;
    if (!('pageSize' in value) || value['pageSize'] === undefined) return false;
    if (!('total' in value) || value['total'] === undefined) return false;
    return true;
}

export function InvitationListFromJSON(json: any): InvitationList {
    return InvitationListFromJSONTyped(json, false);
}

export function InvitationListFromJSONTyped(json: any, ignoreDiscriminator: boolean): InvitationList {
    if (json == null) {
        return json;
    }
    return {
        
        'items': ((json['items'] as Array<any>).map(InvitationItemFromJSON)),
        'page': json['page'],
        'pageSize': json['pageSize'],
        'total': json['total'],
        'sort': json['sort'] == null ? undefined : ((json['sort'] as Array<any>).map(SortSpecFromJSON)),
    };
}

export function InvitationListToJSON(json: any): InvitationList {
    return InvitationListToJSONTyped(json, false);
}

export function InvitationListToJSONTyped(value?: InvitationList | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'items': ((value['items'] as Array<any>).map(InvitationItemToJSON)),
        'page': value['page'],
        'pageSize': value['pageSize'],
        'total': value['total'],
        'sort': value['sort'] == null ? undefined : ((value['sort'] as Array<any>).map(SortSpecToJSON)),
    };
}

