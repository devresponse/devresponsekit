/* tslint:disable */
/* eslint-disable */
/**
 * DevResponse Administrator API
 * Administrator console API (`/api/administrator`). Authenticate EITHER with the Better Auth **session cookie** (send credentials with the request; every **mutation** then also requires an `Origin` or `Referer` header matching a trusted origin — the CSRF guard — which a browser sets itself and a server-side caller must add) OR with a **bearer** API key / JWT, whose effective authority is the intersection of its scopes and the permissions of its owner and which is exempt from the origin guard. Org admins are scoped to their own organization (out-of-scope resources return 404, not 403). Errors use `{ error, message, requestId }`; every response carries an `x-request-id` header.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { UserDetail } from './UserDetail';
import {
    UserDetailFromJSON,
    UserDetailFromJSONTyped,
    UserDetailToJSON,
    UserDetailToJSONTyped,
} from './UserDetail';

/**
 * 
 * @export
 * @interface UserDetailEnvelope
 */
export interface UserDetailEnvelope {
    /**
     * 
     * @type {UserDetail}
     * @memberof UserDetailEnvelope
     */
    user: UserDetail;
}

/**
 * Check if a given object implements the UserDetailEnvelope interface.
 */
export function instanceOfUserDetailEnvelope(value: object): value is UserDetailEnvelope {
    if (!('user' in value) || value['user'] === undefined) return false;
    return true;
}

export function UserDetailEnvelopeFromJSON(json: any): UserDetailEnvelope {
    return UserDetailEnvelopeFromJSONTyped(json, false);
}

export function UserDetailEnvelopeFromJSONTyped(json: any, ignoreDiscriminator: boolean): UserDetailEnvelope {
    if (json == null) {
        return json;
    }
    return {
        
        'user': UserDetailFromJSON(json['user']),
    };
}

export function UserDetailEnvelopeToJSON(json: any): UserDetailEnvelope {
    return UserDetailEnvelopeToJSONTyped(json, false);
}

export function UserDetailEnvelopeToJSONTyped(value?: UserDetailEnvelope | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'user': UserDetailToJSON(value['user']),
    };
}

